import { Text, View } from "react-native";

export default function InterestsScreen() {
  return (
    <View className="flex-1 bg-background items-center justify-center">
      <Text className="text-xl font-bold text-primary">Interests</Text>
    </View>
  );
}